# 46 - 技能系统需求

> **版本**: v1.0
> **最后更新**: 2026-01-29
> **依赖文档**: 18-工具抽象层需求, 19-工具注册与发现需求
> **后续文档**: 无（架构完整）

---

## 1. 需求概述

### 1.1 目标描述

实现**可扩展的技能系统**，允许通过 SKILL.md 文件动态教会 Agent 如何使用外部工具、CLI 命令、API 服务等，无需修改核心代码。

**核心目标:**
- **动态加载**: 支持从多个位置加载 skill，支持优先级覆盖
- **门控机制**: 根据环境、配置、依赖自动过滤 skill
- **热重载**: 监听 skill 文件变化，自动刷新
- **安全隔离**: 控制 skill 可访问的环境变量和工具
- **社区生态**: 支持从 ClawdHub 发现和安装 skill

---

## 2. 核心概念

### 2.1 什么是 Skill？

**Skill** 是一个包含 `SKILL.md` 文件的目录，用于教 Agent 如何使用某个工具。

**目录结构**:
```
skills/
  1password/
    SKILL.md              # 必需：技能说明文档
    references/           # 可选：参考文档
      get-started.md
      cli-examples.md
```

**SKILL.md 格式**:
```markdown
---
name: 1password
description: Set up and use 1Password CLI (op)
homepage: https://developer.1password.com/docs/cli/
metadata: {"moltbot":{"emoji":"🔐","requires":{"bins":["op"]}}}
---

# 1Password CLI

[教学内容：如何使用 op 命令]

## Workflow
1. Check OS + shell
2. Verify CLI present: `op --version`
3. Sign in: `op signin`
...
```

### 2.2 Skill 的作用

```
┌───────────────────────────────────────────┐
│ Agent 收到用户请求                         │
│ "帮我从 1Password 读取密码"               │
└──────────────┬────────────────────────────┘
               │
               ▼
┌───────────────────────────────────────────┐
│ Skill System 提供上下文                    │
│ - 1password skill 已加载                  │
│ - 注入 SKILL.md 内容到系统提示            │
└──────────────┬────────────────────────────┘
               │
               ▼
┌───────────────────────────────────────────┐
│ Agent 理解如何使用                         │
│ - 知道需要 tmux 会话                      │
│ - 知道需要运行 op signin                  │
│ - 知道如何读取秘密                        │
└──────────────┬────────────────────────────┘
               │
               ▼
┌───────────────────────────────────────────┐
│ 调用 bash 工具执行                         │
│ tmux ... op read "op://vault/item/field"  │
└───────────────────────────────────────────┘
```

**与内置工具的区别**:
- **内置工具**: 硬编码的工具实现（如 `read_file`, `bash`）
- **Skill**: 教 Agent 如何使用外部 CLI/API（如 `op`, `gh`, `spotify-player`）

### 2.3 Skill 加载位置

Moltbot 从 **3 个位置** 加载 skill，优先级从高到低：

```
┌──────────────────────────────────────────────────┐
│ 1. Workspace Skills (最高优先级)                 │
│    ~/clawd/skills/                               │
│    - 用户自定义 skill                            │
│    - 覆盖同名的管理/捆绑 skill                    │
├──────────────────────────────────────────────────┤
│ 2. Managed/Local Skills (中等优先级)             │
│    ~/.clawdbot/skills/                           │
│    - 通过 ClawdHub 安装的 skill                  │
│    - 用户手动添加的 skill                        │
├──────────────────────────────────────────────────┤
│ 3. Bundled Skills (最低优先级)                   │
│    <install-dir>/skills/                         │
│    - npm 包或 Moltbot.app 自带的 skill           │
│    - 如 1password, github, summarize             │
└──────────────────────────────────────────────────┘
```

**优先级规则**:
- 如果 3 个位置都有同名 skill，只加载 Workspace 版本
- 用于本地调试/修改 skill

---

## 3. Skill 元数据格式

### 3.1 Frontmatter 字段

**基础字段**:
```markdown
---
name: skill-name          # 必需：技能唯一标识
description: Brief desc   # 必需：简短描述（Agent 看到）
homepage: https://...     # 可选：官方文档链接
user-invocable: true      # 可选：是否允许用户通过 /命令 调用
disable-model-invocation: false  # 可选：是否禁止模型自动调用
metadata: {"moltbot":{...}}      # 可选：Moltbot 特定配置
---
```

**metadata.moltbot 字段**:
```json5
{
  "moltbot": {
    "emoji": "🔐",                    // 可选：UI 显示的 emoji
    "homepage": "https://...",        // 可选：替代 homepage 字段
    "always": true,                   // 可选：跳过所有门控，总是加载
    "os": ["darwin", "linux"],        // 可选：限制操作系统
    "skillKey": "custom-key",         // 可选：配置文件中使用的键
    "primaryEnv": "API_KEY_NAME",     // 可选：主要环境变量名
    "requires": {
      "bins": ["op"],                 // 必需的二进制（全部需要）
      "anyBins": ["npm", "pnpm"],     // 备选二进制（至少一个）
      "env": ["API_KEY"],             // 必需的环境变量
      "config": ["browser.enabled"]   // 必需的配置项
    },
    "install": [                      // 可选：安装器定义
      {
        "id": "brew",
        "kind": "brew",
        "formula": "1password-cli",
        "bins": ["op"],
        "label": "Install 1Password CLI (brew)"
      }
    ]
  }
}
```

### 3.2 门控规则详解

**requires.bins**:
```json5
"requires": {
  "bins": ["op", "jq"]  // op AND jq 都必须在 PATH 中
}
```

**requires.anyBins**:
```json5
"requires": {
  "anyBins": ["npm", "pnpm", "yarn", "bun"]  // 至少一个包管理器
}
```

**requires.env**:
```json5
"requires": {
  "env": ["ANTHROPIC_API_KEY"]  // 环境变量或配置必须提供
}
```

**requires.config**:
```json5
"requires": {
  "config": ["browser.enabled", "channels.discord.token"]
}
```

**os 限制**:
```json5
"os": ["darwin"]  // 仅 macOS
```

### 3.3 安装器定义

**Homebrew 安装器**:
```json5
{
  "id": "brew",
  "kind": "brew",
  "formula": "steipete/tap/summarize",
  "bins": ["summarize"],
  "label": "Install summarize (brew)"
}
```

**npm 安装器**:
```json5
{
  "id": "node",
  "kind": "node",
  "package": "@clawdhub/gifgrep",
  "bins": ["gifgrep"],
  "label": "Install gifgrep (npm)"
}
```

**Go 安装器**:
```json5
{
  "id": "go",
  "kind": "go",
  "package": "github.com/user/tool@latest",
  "bins": ["tool"],
  "label": "Install tool (go)"
}
```

**下载安装器**:
```json5
{
  "id": "download",
  "kind": "download",
  "url": "https://example.com/tool.tar.gz",
  "archive": "tar.gz",
  "extract": true,
  "stripComponents": 1,
  "targetDir": "~/.clawdbot/tools/tool-name",
  "bins": ["tool"],
  "label": "Download tool binary"
}
```

---

## 4. 配置系统

### 4.1 全局配置

**位置**: `~/.clawdbot/moltbot.json`

```json5
{
  "skills": {
    // 允许的捆绑 skill 白名单（可选）
    "allowBundled": [
      "1password",
      "github",
      "summarize"
    ],

    // 加载配置
    "load": {
      "watch": true,              // 监听 skill 文件变化
      "watchDebounceMs": 250,     // 防抖延迟
      "extraDirs": [              // 额外的 skill 目录
        "/shared/skills"
      ]
    },

    // 安装配置
    "install": {
      "nodeManager": "npm"        // npm | pnpm | yarn | bun
    },

    // Skill 特定配置
    "entries": {
      "1password": {
        "enabled": true,          // 启用/禁用
        "apiKey": "sk-...",       // 快捷 API Key 配置
        "env": {                  // 环境变量注入
          "OP_ACCOUNT": "my.1password.com"
        },
        "config": {               // 自定义配置
          "defaultVault": "Personal"
        }
      },
      "summarize": {
        "enabled": true,
        "env": {
          "GEMINI_API_KEY": "AIza..."
        }
      }
    }
  }
}
```

### 4.2 Skill 特定配置

**配置键匹配**:
- 默认使用 skill 的 `name` 字段
- 如果定义了 `metadata.moltbot.skillKey`，使用该键

**示例**:
```markdown
---
name: nano-banana-pro
metadata: {"moltbot":{"skillKey":"gemini-image"}}
---
```

配置使用 `gemini-image` 而非 `nano-banana-pro`:
```json5
{
  "skills": {
    "entries": {
      "gemini-image": {
        "enabled": true,
        "env": {
          "GEMINI_API_KEY": "..."
        }
      }
    }
  }
}
```

### 4.3 环境变量注入

**注入时机**: Agent 运行开始时

**注入逻辑**:
```typescript
// 1. 保存当前环境
const originalEnv = { ...process.env };

// 2. 注入 skill 配置的环境变量
for (const [skillName, config] of Object.entries(skills)) {
  // 注入 apiKey（primaryEnv）
  if (config.apiKey && skill.metadata.moltbot.primaryEnv) {
    const envVar = skill.metadata.moltbot.primaryEnv;
    if (!process.env[envVar]) {
      process.env[envVar] = config.apiKey;
    }
  }

  // 注入自定义 env
  for (const [key, value] of Object.entries(config.env || {})) {
    if (!process.env[key]) {
      process.env[key] = value;
    }
  }
}

// 3. 运行 Agent
await runAgent();

// 4. 恢复原始环境
process.env = originalEnv;
```

**安全注意**:
- 环境变量只在 Agent 运行期间注入
- 不影响 Gateway 进程的全局环境
- 不注入已存在的环境变量（优先系统环境）

---

## 5. 加载流程

### 5.1 启动加载

```
Gateway 启动
    │
    ▼
扫描 3 个 skill 目录
    │
    ├─ Bundled: <install>/skills/
    ├─ Managed:  ~/.clawdbot/skills/
    └─ Workspace: ~/clawd/skills/
    │
    ▼
解析所有 SKILL.md
    │
    ▼
应用优先级规则
（同名 skill 取高优先级版本）
    │
    ▼
应用门控规则
    │
    ├─ 检查 os 限制
    ├─ 检查 bins 依赖
    ├─ 检查 env 变量
    └─ 检查 config 配置
    │
    ▼
应用配置过滤
    │
    ├─ allowBundled 白名单
    └─ entries.*.enabled 开关
    │
    ▼
生成最终 skill 列表
    │
    ▼
缓存到内存
```

### 5.2 会话加载

**快照机制**:
```
新会话创建
    │
    ▼
获取当前有效 skill 列表
    │
    ▼
保存为会话快照
    │
    ▼
后续 turn 复用快照
（无需重新扫描）
```

**刷新时机**:
1. Skill 文件变化（watch 模式）
2. 新的远程节点连接（macOS node on Linux gateway）
3. 手动触发刷新

### 5.3 热重载

**监听模式**:
```json5
{
  "skills": {
    "load": {
      "watch": true,
      "watchDebounceMs": 250
    }
  }
}
```

**工作流程**:
```
SKILL.md 文件修改
    │
    ▼
文件系统事件触发
    │
    ▼
防抖延迟 250ms
    │
    ▼
重新扫描 skill 目录
    │
    ▼
更新 skill 列表版本号
    │
    ▼
下一个 Agent turn 使用新列表
```

---

## 6. 上下文注入

### 6.1 注入格式

当 skill 被加载时，注入到 Agent 系统提示的格式：

```xml
<available_skills>
<skill>
<name>1password</name>
<description>Set up and use 1Password CLI (op)</description>
<location>/Users/.../skills/1password</location>
</skill>
<skill>
<name>summarize</name>
<description>Summarize URLs, podcasts, and local files</description>
<location>/Users/.../skills/summarize</location>
</skill>
</available_skills>
```

**Token 成本**:
- 基础开销（有 skill 时）: ~195 字符
- 每个 skill: ~97 字符 + name + description + location 长度
- 粗略估算: 每个 skill ~24 tokens

### 6.2 Skill 内容注入

**SKILL.md 内容**:
- 完整内容注入到系统提示
- Agent 可以读取 skill 目录中的 reference 文件
- 使用 `{baseDir}` 占位符引用 skill 目录

**示例**:
```markdown
---
name: 1password
---

# References

See `{baseDir}/references/cli-examples.md` for real examples.
```

Agent 可以执行:
```typescript
read_file("/path/to/skills/1password/references/cli-examples.md")
```

---

## 7. 安装器系统

### 7.1 安装器选择逻辑

**优先级**:
1. Homebrew (macOS/Linux)
2. Node (npm/pnpm/yarn/bun)
3. Go
4. uv (Python)
5. Download

**平台过滤**:
```json5
{
  "install": [
    {
      "id": "brew",
      "os": ["darwin", "linux"],
      "kind": "brew",
      "formula": "tool"
    },
    {
      "id": "download-mac",
      "os": ["darwin"],
      "kind": "download",
      "url": "https://example.com/tool-mac.tar.gz"
    },
    {
      "id": "download-linux",
      "os": ["linux"],
      "kind": "download",
      "url": "https://example.com/tool-linux.tar.gz"
    }
  ]
}
```

Gateway 根据当前操作系统选择合适的安装器。

### 7.2 自动安装（macOS App）

**macOS Skills UI 流程**:
```
用户点击 "Install" 按钮
    │
    ▼
Gateway 查找可用安装器
    │
    ▼
选择最佳安装器
（优先 brew > node > ...）
    │
    ▼
执行安装命令
    │
    ├─ brew: brew install <formula>
    ├─ node: npm install -g <package>
    ├─ go: go install <package>
    └─ download: curl + extract
    │
    ▼
验证 bins 是否可用
    │
    ▼
刷新 skill 列表
```

### 7.3 依赖自动安装

**Go 依赖**:
```
检测到 go install 但 go 不存在
    │
    ▼
检查是否有 brew
    │
    ▼
自动运行: brew install go
    │
    ▼
设置 GOBIN 到 Homebrew bin
    │
    ▼
继续执行 go install
```

---

## 8. ClawdHub 集成

### 8.1 ClawdHub 是什么？

**定位**: Moltbot 的技能注册中心（类似 npm registry）

**网站**: https://clawdhub.com

**功能**:
- 浏览所有公开 skill
- 安装 skill 到本地
- 更新已安装的 skill
- 发布自己的 skill

### 8.2 安装 Skill

**命令行**:
```bash
# 安装到当前目录的 skills/
clawdhub install <skill-slug>

# 安装到指定目录
clawdhub install <skill-slug> --dir ~/.clawdbot/skills
```

**工作流程**:
```
clawdhub install summarize
    │
    ▼
从 ClawdHub API 获取 skill 元数据
    │
    ▼
下载 skill 文件
    │
    ▼
解压到 ./skills/summarize/
    │
    ▼
验证 SKILL.md 格式
    │
    ▼
完成（下次 Agent 运行时自动加载）
```

### 8.3 更新 Skill

**单个更新**:
```bash
clawdhub update summarize
```

**批量更新**:
```bash
clawdhub update --all
```

### 8.4 发布 Skill

**前提**:
1. 创建 skill 目录和 SKILL.md
2. 注册 ClawdHub 账号

**流程**:
```bash
# 扫描本地 skills/
clawdhub scan

# 发布到 ClawdHub
clawdhub publish <skill-name>

# 或一键同步（扫描 + 发布更新）
clawdhub sync --all
```

---

## 9. 实战示例

### 9.1 示例 1: 1Password Skill

**需求**: 让 Agent 能够从 1Password 读取密码

**SKILL.md**:
```markdown
---
name: 1password
description: Set up and use 1Password CLI (op)
metadata: {"moltbot":{"emoji":"🔐","requires":{"bins":["op"]}}}
---

# 1Password CLI

## REQUIRED: tmux session

All `op` commands must run inside a fresh tmux session.

Example:
\```bash
SOCKET_DIR="${TMPDIR:-/tmp}/moltbot-tmux-sockets"
mkdir -p "$SOCKET_DIR"
SOCKET="$SOCKET_DIR/moltbot-op.sock"
SESSION="op-auth-$(date +%Y%m%d-%H%M%S)"

tmux -S "$SOCKET" new -d -s "$SESSION" -n shell
tmux -S "$SOCKET" send-keys -t "$SESSION" "op signin" Enter
tmux -S "$SOCKET" send-keys -t "$SESSION" "op whoami" Enter
\```

## Read secrets

\```bash
op read "op://Private/Npmjs/one-time password?attribute=otp"
\```
```

**使用场景**:
```
用户: "帮我从 1Password 读取 npm 的 OTP"

Agent:
1. 读取 1password skill
2. 理解需要 tmux 会话
3. 执行:
   - 创建 tmux 会话
   - 运行 op signin
   - 运行 op read "op://Private/Npmjs/one-time password?attribute=otp"
   - 返回 OTP 给用户
```

### 9.2 示例 2: Summarize Skill

**需求**: 让 Agent 能够总结网页/视频内容

**SKILL.md**:
```markdown
---
name: summarize
description: Summarize URLs, podcasts, and local files
metadata: {"moltbot":{"emoji":"🧾","requires":{"bins":["summarize"]}}}
---

# Summarize

## Quick start

\```bash
summarize "https://example.com" --model google/gemini-3-flash-preview
summarize "https://youtu.be/dQw4w9WgXcQ" --youtube auto
\```

## YouTube transcript

\```bash
summarize "https://youtu.be/..." --youtube auto --extract-only
\```
```

**配置**:
```json5
{
  "skills": {
    "entries": {
      "summarize": {
        "enabled": true,
        "env": {
          "GEMINI_API_KEY": "AIzaSy..."
        }
      }
    }
  }
}
```

**使用场景**:
```
用户: "总结这个视频 https://youtu.be/..."

Agent:
1. 识别到 summarize skill
2. 环境变量 GEMINI_API_KEY 自动注入
3. 执行: summarize "https://youtu.be/..." --youtube auto
4. 返回总结给用户
```

### 9.3 示例 3: 自定义 Skill

**需求**: 创建一个 Slack 消息发送 skill

**目录结构**:
```
~/clawd/skills/
  slack-send/
    SKILL.md
    examples/
      send-message.sh
```

**SKILL.md**:
```markdown
---
name: slack-send
description: Send messages to Slack channels via webhook
metadata: {"moltbot":{"requires":{"env":["SLACK_WEBHOOK_URL"]}}}
---

# Slack Send

Send messages to Slack via webhook.

## Usage

\```bash
curl -X POST "$SLACK_WEBHOOK_URL" \
  -H 'Content-Type: application/json' \
  -d '{"text":"Hello from Moltbot!"}'
\```

## Examples

See `{baseDir}/examples/send-message.sh` for more examples.
```

**配置**:
```json5
{
  "skills": {
    "entries": {
      "slack-send": {
        "enabled": true,
        "env": {
          "SLACK_WEBHOOK_URL": "https://hooks.slack.com/services/..."
        }
      }
    }
  }
}
```

**使用**:
```
用户: "发个消息到 Slack 说任务完成了"

Agent:
1. 读取 slack-send skill
2. 获取 SLACK_WEBHOOK_URL
3. 执行 curl 命令发送消息
```

---

## 10. 高级特性

### 10.1 用户调用 vs 模型调用

**user-invocable**:
- `true` (默认): 用户可通过 `/skill-name` 命令调用
- `false`: 只有模型可以使用

**disable-model-invocation**:
- `false` (默认): 模型可以自动使用
- `true`: 只能通过用户命令调用

**场景**:
```markdown
---
name: admin-tool
user-invocable: true
disable-model-invocation: true
---
```

效果:
- 用户可以输入 `/admin-tool` 手动触发
- 模型不会自动调用这个 skill

### 10.2 命令分发

**直接工具调用模式**:
```markdown
---
name: weather
command-dispatch: tool
command-tool: weather_get
command-arg-mode: raw
---
```

**工作流程**:
```
用户输入: /weather Tokyo

Gateway 直接调用工具:
weather_get({
  command: "Tokyo",
  commandName: "weather",
  skillName: "weather"
})

跳过 Agent 推理
```

### 10.3 远程节点 Skill

**场景**: Linux Gateway + macOS Node

```
Gateway (Linux)
    │
    ├─ 加载 Linux skill
    │
    └─ 检测到 macOS node 连接
        │
        ▼
    探测 node 上的二进制
    (system.run "which spotify-player")
        │
        ▼
    加载 macOS-only skill
    (如 spotify-player, Things)
        │
        ▼
    Agent 可通过 nodes.run 调用
```

**实现**:
```typescript
// Gateway 检测 node 能力
if (node.os === "darwin" && node.commands.includes("system.run")) {
  // 探测 macOS 二进制
  const hasSpotify = await probeNodeBinary(node, "spotify-player");
  if (hasSpotify) {
    // 加载 spotify-player skill
    loadSkill("spotify-player");
  }
}
```

---

## 11. 最佳实践

### 11.1 Skill 设计原则

**DO**:
✅ 提供清晰的使用示例
✅ 说明所有必需的依赖
✅ 包含错误处理指南
✅ 使用 `{baseDir}` 引用相对路径
✅ 提供 references/ 目录存放详细文档

**DON'T**:
❌ 假设环境已配置（明确说明所有前置条件）
❌ 直接粘贴密钥（使用环境变量）
❌ 忽略错误场景
❌ 写太长的 SKILL.md（核心内容放 md，详细内容放 references/）

### 11.2 安全最佳实践

**秘密管理**:
```json5
// 好的做法
{
  "skills": {
    "entries": {
      "my-skill": {
        "env": {
          "API_KEY": "secret-value"  // 从配置文件读取
        }
      }
    }
  }
}
```

```markdown
// 不好的做法
---
description: Use API with key: sk-1234567890  ❌ 不要硬编码
---
```

**工具权限**:
```json5
// 限制 skill 可用的工具
{
  "agents": {
    "defaults": {
      "tools": {
        "policy": {
          "default": "deny",
          "allowlist": ["bash", "read_file"]  // 只允许这些工具
        }
      }
    }
  }
}
```

### 11.3 测试 Skill

**手动测试**:
```bash
# 1. 安装依赖
brew install tool-name

# 2. 配置环境变量
export API_KEY="..."

# 3. 测试命令
tool-name --help

# 4. 启动 Gateway，测试 skill
moltbot agent --message "使用 tool-name 做 XXX"
```

**自动化测试**:
```bash
# 创建测试脚本
cat > test-skill.sh <<'EOF'
#!/bin/bash
set -e

# 测试依赖
which tool-name || { echo "Missing tool-name"; exit 1; }

# 测试基本功能
output=$(tool-name test-command)
echo "$output" | grep "expected-output" || {
  echo "Test failed"
  exit 1
}

echo "✅ Skill test passed"
EOF

chmod +x test-skill.sh
./test-skill.sh
```

---

## 12. 故障排查

### 12.1 Skill 未加载

**检查清单**:
```bash
# 1. 检查文件是否存在
ls ~/clawd/skills/skill-name/SKILL.md

# 2. 检查 YAML frontmatter 格式
head -n 10 ~/clawd/skills/skill-name/SKILL.md

# 3. 检查依赖
which required-binary

# 4. 检查环境变量
echo $REQUIRED_ENV_VAR

# 5. 检查配置
moltbot config get skills.entries.skill-name.enabled

# 6. 查看日志
tail -f ~/.clawdbot/logs/gateway.log | grep skill
```

### 12.2 Skill 加载但不工作

**调试步骤**:
```bash
# 1. 手动测试命令
command-from-skill --test

# 2. 在 Agent 中测试
moltbot agent --message "测试 skill-name"

# 3. 查看详细日志
moltbot agent --message "测试 skill-name" --verbose
```

### 12.3 常见错误

**错误 1: metadata 格式错误**
```markdown
❌ 错误
metadata: {
  "moltbot": {
    "requires": {"bins": ["op"]}
  }
}

✅ 正确（单行 JSON）
metadata: {"moltbot":{"requires":{"bins":["op"]}}}
```

**错误 2: bins 依赖缺失**
```
错误: Skill 'tool-name' requires binary 'missing-tool'

解决:
brew install missing-tool
```

**错误 3: 环境变量缺失**
```
错误: Skill 'api-client' requires env var 'API_KEY'

解决:
{
  "skills": {
    "entries": {
      "api-client": {
        "env": { "API_KEY": "your-key" }
      }
    }
  }
}
```

---

## 13. 与 Claude Code 对比

### 13.1 Claude Code 没有 Skill 系统

**Claude Code 的工具系统**:
- **硬编码工具**: Read, Edit, Write, Bash, Glob, Grep
- **CLAUDE.md**: 项目级指令文件
- **无动态扩展**: 不能通过配置文件添加新工具

**对比**:
| 维度 | Moltbot | Claude Code |
|------|---------|-------------|
| **工具扩展** | 动态加载 skill | 固定工具集 |
| **外部 CLI** | 通过 skill 教学 | 直接调用 bash |
| **知识注入** | SKILL.md → 系统提示 | CLAUDE.md → 系统提示 |
| **社区生态** | ClawdHub 共享 | 无 |

### 13.2 为什么 Moltbot 需要 Skill 系统？

**原因 1: 多样化的工具需求**
```
Moltbot 用户场景:
- 使用 1Password 管理密码
- 使用 Spotify 播放音乐
- 使用 Things 管理任务
- 使用 Bear Notes 记笔记
...

Claude Code 用户场景:
- 读写代码文件
- 运行 git/npm 命令
- 搜索代码库
（固定工具即可满足）
```

**原因 2: 长期使用 vs 临时使用**
- **Moltbot**: 长期运行，用户会安装自定义工具
- **Claude Code**: 临时使用，每次运行针对当前项目

**原因 3: 社区贡献**
- **Moltbot**: ClawdHub 生态，用户可分享 skill
- **Claude Code**: 个人工具，无社区生态需求

---

## 14. 总结

### 14.1 Skill 系统核心价值

**1. 可扩展性**
- 无需修改核心代码即可添加新功能
- 用户可自定义 skill

**2. 知识传递**
- SKILL.md 教会 Agent 如何使用工具
- Agent 不需要预训练该工具的知识

**3. 社区生态**
- ClawdHub 共享 skill
- 降低使用门槛

**4. 配置灵活性**
- 门控规则自动过滤不适用的 skill
- 环境变量注入简化配置

### 14.2 设计亮点

**1. 分层加载**
```
Workspace → Managed → Bundled
允许用户覆盖默认 skill
```

**2. 智能门控**
```
根据 bins/env/config 自动判断可用性
减少配置负担
```

**3. 安全隔离**
```
环境变量仅在 Agent 运行期间注入
不污染 Gateway 进程
```

**4. 热重载**
```
监听文件变化，自动刷新
无需重启 Gateway
```

### 14.3 适用场景

**✅ 适合用 Skill**:
- 封装复杂的 CLI 工具使用
- 多步骤流程（如 op signin → op read）
- 需要特殊环境设置（如 tmux）
- 可复用的工具集成

**❌ 不适合用 Skill**:
- 简单的 bash 命令（直接用 bash 工具）
- 一次性脚本
- 核心功能（应该做成内置工具）

---

**文档完成** ✅